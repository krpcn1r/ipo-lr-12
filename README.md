# Лабораторная работа №12
## Задание 1
Создать класс **Client** для клиентов компании.

### Атрибуты:
- `name` — имя клиента  
- `cargo_weight` — вес груза  
- `is_vip` — флаг VIP-статуса (по умолчанию `False`)

---

## Задание 2
Создать базовый класс **Vehicle** для транспортного средства.

### Атрибуты:
- `vehicle_id` — уникальный идентификатор (строка, генерируется случайно при создании)  
- `capacity` — грузоподъемность (в тоннах)  
- `current_load` — текущая загрузка (по умолчанию `0`)  
- `clients_list` — список клиентов, чьи грузы загружены  

### Методы:
- `load_cargo(client)` — увеличивает загрузку на `cargo_weight` из объекта `client` и добавляет клиента в список клиентов  
  - Проверка на превышение грузоподъемности  
  - Валидация типов данных  
- Магический метод `__str__` — возвращает строку с ID транспорта, его грузоподъемностью и текущей загрузкой  

---

## Задание 4
После выполнения индивидуальных заданий необходимо разработать меню (как в 8-й лабораторной работе) в файле **main.py** для удобной работы с клиентами, грузами, а также возможностью вывода результата распределения грузов.

---

## Вариант 2

### 1. Класс **Ship** (судно)
Наследуется от `Vehicle`.

#### Дополнительные атрибуты:
- `name` — название судна  

---

### 2. Класс **Truck** (грузовик)
Наследуется от `Vehicle`.

#### Дополнительные атрибуты:
- `color` — цвет грузовика  

---

### 3. Класс **TransportCompany**
Класс для управления транспортной компанией.

#### Атрибуты:
- `name` — название компании  
- `vehicles` — список транспортных средств  
- `clients` — список клиентов  

#### Методы:
- `add_vehicle(vehicle)` — добавляет транспортное средство (с валидацией)  
- `list_vehicles()` — возвращает список всех транспортных средств  
- `add_client(client)` — добавляет клиента  
- `optimize_cargo_distribution()` — распределяет грузы клиентов по транспортным средствам с учетом правил:
  - Грузы VIP-клиентов загружаются в первую очередь  
  - Для загрузки используется как можно меньше транспорта  

---

# Лабораторная работа №13

## Задание 1
Создать в репозитории новую ветку **gui** для выполнения заданий.

---

## Задание 2
Разработать графический пользовательский интерфейс вместо консольного для лабораторной работы №11 в файле **main_gui.py**.

### Требования к интерфейсу

#### 1. Основные окна и их структура
- **Главное окно приложения**
  - **Меню:**
    - "Экспорт результата" — сохранение распределения грузов в файл (см. Задание 3), с проверкой на наличие данных.
    - "О программе" — модальное окно с номером лабораторной работы, вариантом и ФИО разработчика.
  - **Рабочая область:**
    - Панель управления объектами (кнопки).
    - Таблицы данных (клиенты и транспортные средства).
  - **Статусная строка:**
    - Отображает текущие действия и сообщения об ошибках.
  - **Кнопки управления:**
    - "Добавить клиента"
    - "Добавить транспорт"
    - "Распределить грузы"

- **Окно добавления/редактирования клиента**
  - Поля для ввода данных клиента.
  - Кнопки: "Сохранить", "Отмена".

- **Окно добавления/редактирования транспортного средства**
  - Поля для ввода данных транспорта.
  - Кнопки: "Сохранить", "Отмена".

---

#### 2. Поля для ввода данных и их валидация
- **Для клиента:**
  - Имя клиента — обязательное, текстовое (валидация: только буквы, минимум 2 символа).
  - Вес груза — обязательное, числовое (валидация: положительное число, не более 10000 кг).
  - VIP статус — чекбокс (по умолчанию отключен).

- **Для транспортного средства:**
  - Тип транспорта — выпадающий список (варианты: "Грузовик", "Поезд").
  - Дополнительные поля (грузоподъемность, цвет/название и т.д.).

- **Общее для всех форм:**
  - При некорректном вводе появляется окно с предупреждением и поле очищается.

---

#### 3. Действия в интерфейсе
- **Добавление объекта:**
  - Клиента — открывается окно для заполнения данных и сохранения.
  - Транспортного средства — аналогично, с выбором типа транспорта.
- **Редактирование объекта:**
  - По двойному щелчку на записи открывается окно редактирования.
- **Удаление объекта:**
  - Выбор из таблицы и нажатие кнопки "Удалить".
- **Оптимизация распределения грузов:**
  - Кнопка "Распределить грузы".
  - После выполнения отображается таблица с результатами.
- **Сохранение и загрузка данных:**
  - Сохранение текущего состояния в файл.

---

#### 4. Дополнительные рекомендации
- **Удобство работы:**
  - Всплывающие подсказки (tooltip) для всех кнопок и полей.
  - Управление клавишами (Enter — подтверждение, Esc — отмена).
- **Сообщения:**
  - Уведомления об успешных действиях (например, "Клиент добавлен").
  - Ошибки в модальном окне (например, "Грузоподъемность не может быть меньше 0").
- **Отображение данных:**
  - Таблица клиентов: имя, вес груза, статус VIP.
  - Таблица транспортных средств: ID, тип, грузоподъемность, текущая загрузка.
  - Возможность сортировки и фильтрации по столбцам.
- **Логика оптимизации:**
  - Результат распределения грузов отображается в отдельном модальном окне или в основной таблице.

---

## Задание 3
Добавить возможность сохранения результатов распределения грузов в файл (формат на выбор: CSV, JSON, TXT и т.д.).  
В интерфейсе предусмотреть кнопку для выполнения операции.

---

## Задание 4
Совершить слияние веток **main** и **gui**.  

